<UserControl x:Class="PrototypeUI_1.View.AutoAssembleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" >
    <Grid>
        <Border Background="Black" Opacity="0.2"/>
        <Grid Width="554" Height="707">
            <Grid.Background>
                <ImageBrush ImageSource="/PrototypeUI_1;component/Images/background_v3.png"/>
            </Grid.Background>

            <Border Width="472" Height="472" VerticalAlignment="Top" Margin="0,33,0,0" Background="#D8D8D8"></Border>
            <ProgressBar Value="48" VerticalAlignment="Top" Margin="75,525,75,0"/>
            <TextBlock Text="48%" VerticalAlignment="Top" Margin="0,555,0,0" HorizontalAlignment="Center" FontSize="31" FontWeight="Bold" Foreground="{StaticResource FgColor}"/>
            <Button Margin="0,618,0,0" Width="180" VerticalAlignment="Top" Style="{StaticResource X_Button_g}"  Content="完成装配"/>
        </Grid>
    </Grid>
</UserControl>
