<UserControl x:Class="PrototypeUI_1.Views.BomReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrototypeUI_1.Views"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Button Style="{StaticResource X_Button_Browser}"  HorizontalAlignment="Left"></Button>
        <Grid Grid.Row="2" Grid.Column="2">
            <Button Style="{StaticResource X_Button}" Content="输出" HorizontalAlignment="Left"></Button>
            <Button Style="{StaticResource X_Button_g}" Content="取消" HorizontalAlignment="Left" Margin="180,0,0,0"></Button>
        </Grid>
       
    </Grid>
</UserControl>
